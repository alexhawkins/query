<div class="row">
  <div class="col-md-6 col-md-offset-1">
  <h3><%= @topic.title %></h3>
    <% @questions.each do |question| %>
      <div class="media">
        <div class="media-body">
          <p>
            Question added to <%= link_to @topic.title, topic_path(@topic) %> <%= link_to time_ago_in_words(question.created_at), topic_question_path(@topic, question), class: "label label-default" %>
          </p>
          <h4 class="media-heading">
            <%= link_to question.title,  topic_question_path(@topic, question) %>
          </h4> 
           <small>
              submitted <%= time_ago_in_words(question.created_at) %> ago by <%= question.user.name %>
          </small>
          <p><%= question.body %></p>

           <h5><%= link_to "#{question.answers.count} Answers" , topic_question_path(@topic, question) %></h5>
              
        </div>
      </div>
      <hr>
    <% end %>
  </div>

  <!--Sidebar Column -->
  <div class="col-md-4 col-md-offset-1">
      <div class="list-group">
      <a href="#" class="list-group-item active">
        Setup Your Account
      </a>
      <a href="#" class="list-group-item">Visit your feed</a>
      <a href="#" class="list-group-item">Follow more topics</a>
      <a href="#" class="list-group-item">Find your friends on Query</a>
      <a href="#" class="list-group-item">Upvote answers you like</a>
      <a href="#" class="list-group-item">Ask your first question</a>
      <a href="#" class="list-group-item">Add details about what you know</a>
      <a href="#" class="list-group-item">Answer a question</a>
    </div>
    <div class="list-group">
    <a href="#" class="list-group-item active">
      <h4 class="list-group-item-heading">Follow Topic</h4>
      <p class="list-group-item-text">
        While not always necessary, sometimes you need to put your DOM in a box. For those situations, try the panel component.
      </p>
    </a>
    <a href="#" class="list-group-item">
      <h4 class="list-group-item-heading">Share Topic</h4>
      <p class="list-group-item-text">
        While not always necessary, sometimes you need to put your DOM in a box. For those situations, try the panel component.
      </p>
    </a>
  </div>
  <!---RELATED QUESTIONS -->
  <h3 class="page-header">Related Topics</h3>
    <div class="list-group">
      <!--loop through each question associated with the Topic object-->
      <% @topic.questions.each do |question| %>
        <!--of the related questions, find the other topics they share-->
        <% question.topics.each do |topic| %>
          <%= link_to topic.title, topic_path(topic), class: 'list-group-item'%>
        <% end %>
      <% end %>
    </div>
  </div>
</div>
