<!-- QUESTION TITLE -->
<h2>
  <strong><%= @question.title %></strong>
  <!-- show an edit button if user is a mebmer of the site -->
  <% if policy(@question).edit? %>
    <%= link_to "Edit", edit_question_path(@question),  class: 'btn btn-success btn-xs', remote: true %>
  <% end %> 
</h2>
<!-- QUESTION DETAILS -->
<p class="lead">
  <%= @question.body %>
  <!-- show an edit button if user is a mebmer of the site -->
  <% if policy(@question).edit? %>
    <%= link_to "Edit", edit_question_path(@question),  class: 'btn btn-success btn-xs', remote: true %>
  <% end %>
  </p>